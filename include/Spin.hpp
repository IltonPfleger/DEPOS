#pragma once

struct Spin {

    // Semaphore(int value) : RawSemaphore(value) {}

    // void p() {
    //     CPU::Interrupt::disable();
    //     RawSemaphore::p();
    //     CPU::Interrupt::enable();
    // }

    // void v() {
    //     CPU::Interrupt::disable();
    //     RawSemaphore::v();
    //     CPU::Interrupt::enable();
    // }
};
